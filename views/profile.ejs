<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-gray-100">
    <nav class="bg-blue-600 dark:bg-gray-800 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a class="text-white text-lg font-bold" href="#">ZakMart</a>
            <button class="lg:hidden text-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon">☰</span>
            </button>
            <div class="hidden lg:flex" id="navbarNav">
                <ul class="flex space-x-4">
                    <li>
                        <a href="/products"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Home</button></a>
                    </li>

                    <% if(user){ %>
                        <li>
                            <a href="/profile"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Profile</button></a>
                        </li>
                        <li>
                            <a href="/cart"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Cart</button></a>
                        </li>                            
                        <li>
                            <a href="/logout"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Log Out</button></a>
                        </li>
                    <% } else{ %>
                        <li>
                            <a href="/"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Login</button></a>
                        </li>
                        <li>
                            <a href="/signup"><button class="btn bg-yellow-400 hover:bg-black hover:text-white text-black py-2 px-4 rounded">Sign Up</button></a>
                        </li>
                    <% } %>

                </ul>
            </div>
        </div>
    </nav>

    <div class="container mx-auto mt-5">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div class="col-span-1">
                <div class="bg-gray-800 p-4 rounded-lg shadow-md text-center">
                    <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-chat/ava3.webp" class="w-48 h-48 rounded-full mx-auto" alt="Profile Picture">
                    <div class="mt-4">
                        <h5 class="text-xl font-bold"><%= user %></h5>
                        <p class="text-gray-400">Email: <%= email %></p>
                        <p class="text-gray-400">Phone: <%= number %></p>
                        <a href="/edit" class="mt-4 inline-block bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700">Edit Profile</a>
                    </div>
                </div>
            </div>
            <div class="col-span-2">
                <div class="bg-gray-800 p-4 rounded-lg shadow-md">
                    <h5 class="text-xl font-bold mb-4">Order History</h5>
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr>
                                <th class="border-b-2 border-gray-700 p-2">#</th>
                                <th class="border-b-2 border-gray-700 p-2">Order ID</th>
                                <th class="border-b-2 border-gray-700 p-2">Name</th>
                                <th class="border-b-2 border-gray-700 p-2">Quantity</th>
                                <th class="border-b-2 border-gray-700 p-2">Total Amount</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% let index=1 %>
                            <% result.forEach((i) => { %>
                                <tr>
                                    <td class="border-b border-gray-700 p-2"><%= index %></td>
                                    <td class="border-b border-gray-700 p-2"><%= i.order_id %></td>
                                    <td class="border-b border-gray-700 p-2"><%= i.prod_name %></td>
                                    <td class="border-b border-gray-700 p-2"><%= i.quantity %></td>
                                    <td class="border-b border-gray-700 p-2"><%= i.price %></td>
                                </tr>
                                <% index = index + 1 %>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
</body>
</html>
